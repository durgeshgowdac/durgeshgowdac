name: Generate Snake

on:
  workflow_dispatch: # for manual workflow trigger
  schedule:
    - cron: '0 8 * * *' # runs at every 8AM UTC

jobs:
  build:
    name: Jobs to update snake grid
    runs-on: ubuntu-latest
    steps:
      - uses: Platane/snk/svg-only@v3
        with:
          github_user_name: ${{ github.repository_owner }}

          outputs: |
            dist/github-snake.svg?color_snake=#FF2E63&color_dots=#FFE5EC,#FFB3C6,#FF6B9D,#FF2E63,#C9184A
            dist/ocean.svg?color_snake=#A855F7&color_dots=#0d0a1a,#2d1060,#6020b0,#8f38e0,#A855F7
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - uses: crazy-max/ghaction-github-pages@v5.0.0
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
