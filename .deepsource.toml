version = 1

exclude_patterns = [
    "**/README.md",        # Personal profile readme (no code to analyze)
    "**/LICENSE"           # Standard license file
]

# Enable the secrets analyzer (critical for any repository)
[[analyzers]]
name = "secrets"
enabled = true
